web: python -c "import sys; sys.path.insert(0, './flask_app'); from flask_app.App import create_app; app = create_app('production'); from flask_migrate import upgrade; from flask_sqlalchemy import SQLAlchemy; db = SQLAlchemy(app); with app.app_context(): upgrade()" && python -c "import sys; sys.path.insert(0, './flask_app'); from flask_app.App import create_app; app = create_app('production'); from flask_app.commands import seed_db; with app.app_context(): seed_db()" && gunicorn flask_app.App:app
